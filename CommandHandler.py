

def print_chat_history(history):
    # 在这里执行打印聊天记录的操作

    return history


def reset():
    print()


def help(self):
    return commands
# 创建 CommandHandler 的实例
# history="你好呀"
# text = "/聊天记录"
# command_handler = CommandHandler(history)
# command_handler.handle_command(text)
# 在适当的地方调用 handle_command，例如在收到消息时
# 假设从 MessageExtractor 提取的文本为 text
